name: ambient-snapshot
# Hourly polling during the active registration period (May 11-15), with a
# denser every-10-min boost during the registration days themselves. The
# dedicated hot-window workflow takes over for the minute-by-minute polling
# at noon ET; both can safely overlap because of the concurrency group below.
#
# Outside May 11-15, daily polling is handled by tail.yml (May 16 - July 10),
# after which nothing runs — every class will be Unavailable by then.

on:
  schedule:
    - cron: "0 * 11-15 5 *"           # hourly, May 11-15
    - cron: "*/10 * 11-14 5 *"        # every 10 min, May 11-14 (registration crunch)
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: scrape-write
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Refresh per-section type_code (metadata-only catalog pass)
        run: python -m scraper.scrape --mode types

      - name: Scrape all three registration events
        env:
          ALERT_GMAIL_USER: ${{ secrets.ALERT_GMAIL_USER }}
          ALERT_GMAIL_PASS: ${{ secrets.ALERT_GMAIL_PASS }}
          ALERT_WEBHOOK_URL: ${{ secrets.ALERT_WEBHOOK_URL }}
        run: |
          set -e
          for ev in ENJOYSUMMER1 ENJOYSUMMER2 ENJOYSUMMER; do
            python -m scraper.scrape --mode event --event "$ev"
          done

      - name: Render CSV + HTML
        run: python -m scraper.dump

      - name: Commit snapshot + dumps
        run: |
          git config user.email "actions@github.com"
          git config user.name "github-actions"
          git add data/snapshots.sqlite data/latest.csv data/snapshots.csv index.html
          if git diff --staged --quiet; then
            echo "no change"
          else
            git pull --rebase --autostash
            git commit -m "ambient $(date -u +%FT%TZ)"
            git push
          fi
